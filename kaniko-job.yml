apiVersion: batch/v1
kind: Job
metadata:
  name: kaniko-myportfolio-message-producer
spec:
  parallelism: 1    
  completions: 1    
  template:         
    metadata:
      name: kaniko-myportfolio-message-producer
    spec:
      containers:
      - name: kaniko
        image: gcr.io/kaniko-project/executor:latest
        args: [
          "--dockerfile=/godata/pipelines/myportfolio-message-producer/Dockerfile",
          "--context=/godata/pipelines/myportfolio-message-producer",
          "--destination=nexus-registry-nexus.apps.aws2-dev.ocp.14west.io/myportfolio-message-producer:1.13.1"
        ]
        volumeMounts:
        - mountPath: /godata
          name: gocdagent-data
        - mountPath: /kaniko/.docker/config.json
          name: docker-creds
          subPath: config.json
        env:
        - name: DESITINATION
        value: nexus-registry-nexus.apps.aws2-dev.ocp.14west.io/myportfolio-message-producer:1.13.1
      restartPolicy: Never   
      volumes:
      - name: gocdagent-data
        persistentVolumeClaim:
          claimName: gocdagent
      - name: docker-creds
        configMap:
          defaultMode: 420
          name: configcontent
      backoffLimit: 1
